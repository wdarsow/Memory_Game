import React from 'react';
import "../styles/Navbar.css";

const Navbar = () => (
    <header className="header">
        <h1 className="mainHeader">Memory Game!</h1>
        <h2 className="secondaryHeader">Click on an image to earn points but don't click on any images more than once!</h2>
    </header>
)

export default Navbar;
