import React from 'react';
import "../styles/Navbar.css";

const Navbar = () => (
    <div className="navbarDiv">
        <h1 className="mainHeader">Memory Game!</h1>
        <h3 className="secondaryHeader">Click on an image to earn points but don't click on any images more than once!</h3>
    </div>
)

export default Navbar;
